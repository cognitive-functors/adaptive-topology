# Система типов: MALL с зависимыми типами, (ко)индуктивными типами и дифференцированием

## 1. Введение

Данная система типов представляет собой расширение мультипликативно-аддитивной линейной логики (MALL) с следующими компонентами:
- **Зависимые типы** на основе четырёх линейных связок
- **(Ко)индуктивные типы**, определяемые через выражения MALL с зависимыми типами
- **Дифференцирование** в стиле дифференциальной линейной логики (DILL)

## 2. Базовые связки MALL

### 2.1 Мультипликативные связки

#### Тензорное произведение (⊗)

**Введение**:
```
Γ ⊢ t : A Δ ⊢ u : B
───────────────────────
Γ, Δ ⊢ t ⊗ u : A ⊗ B
```

**Устранение**:
```
Γ ⊢ t : A ⊗ B Δ, x:A, y:B ⊢ u : C
────────────────────────────────────
Γ, Δ ⊢ let x ⊗ y = t in u : C
```

**Семантика**: Линейное произведение ресурсов

#### Par (⅋)

**Введение**:
```
Γ, x:A, y:B ⊢ t : C
────────────────────────────
Γ ⊢ λ(x ⅋ y).t : A ⅋ B ⊸ C
```

> **Примечание**: Данное правило введения для ⅋ отличается от стандартного правила MILL, где введение Par порождает суждение вида `Γ ⊢ t : A ⅋ B` при разделённых контекстах. Здесь используется альтернативная формулировка через ⊸-абстракцию, что позволяет работать с Par конструктивно в рамках системы с зависимыми типами. Стандартное введение Par в MALL выглядит так: из `Γ ⊢ A` и `Δ ⊢ B` выводится `Γ, Δ ⊢ A ⅋ B` (что двойственно устранению ⊗).

**Устранение**: Дуально тензору через линейное отрицание

**Семантика**: Параллельная композиция

#### Мультипликативная единица (1) и bottom (⊥)

**1**:
```
────────────
· ⊢ () : 1
```

**⊥**: Дуальна единице

### 2.2 Аддитивные связки

#### Аддитивная конъюнкция (&)

**Введение**:
```
Γ ⊢ t : A Γ ⊢ u : B
──────────────────────
Γ ⊢ (t, u) : A & B
```

**Устранение**:
```
Γ ⊢ t : A & B Γ ⊢ t : A & B
───────────── ─────────────
Γ ⊢ π₁(t) : A Γ ⊢ π₂(t) : B
```

**Семантика**: Недетерминированный выбор контекста

#### Аддитивная дизъюнкция (⊕)

**Введение**:
```
Γ ⊢ t : A Γ ⊢ t : B
────────────── ──────────────
Γ ⊢ inl(t) : A ⊕ B Γ ⊢ inr(t) : A ⊕ B
```

**Устранение**:
```
Γ ⊢ t : A ⊕ B Δ, x:A ⊢ u : C Δ, y:B ⊢ v : C
──────────────────────────────────────────────────
Γ, Δ ⊢ case t of inl(x) ⇒ u | inr(y) ⇒ v : C
```

**Семантика**: Детерминированный выбор значения

#### Аддитивные единицы (⊤, 0)

**⊤**:
```
─────────────
Γ ⊢ * : ⊤
```

**0**: Нет правил введения

## 3. Зависимые типы

### 3.1 Зависимое тензорное произведение (Σ⊗)

**Введение**:
```
Γ ⊢ t : A Γ ⊢ u : B[t/x]
────────────────────────────────
Γ ⊢ (t ⊗_Σ u) : Σ⊗(x:A).B
```

**Устранение**:
```
Γ ⊢ p : Σ⊗(x:A).B Δ, x:A, y:B ⊢ C : Type Δ, x:A, y:B ⊢ t : C
────────────────────────────────────────────────────────────────────
Γ, Δ ⊢ let_Σ⊗ (x ⊗ y) = p in t : C[p/x⊗y]
```

**Свойства**:
- Линейное использование зависимости
- Первая компонента влияет на тип второй компоненты
- Оба компонента используются линейно

### 3.2 Зависимое par (Π⅋)

**Введение**:
```
Γ, x:A ⊢ t : B
──────────────────────
Γ ⊢ λ⅋x.t : Π⅋(x:A).B
```

**Устранение**:
```
Γ ⊢ f : Π⅋(x:A).B Δ ⊢ a : A
─────────────────────────────────
Γ ⅋ Δ ⊢ f ⅋ a : B[a/x]
```

**Свойства**:
- Параллельное применение
- Контексты объединяются через par
- Зависимость от аргумента

### 3.3 Зависимая аддитивная конъюнкция (Π&)

**Введение** (для булева индекса):
```
Γ ⊢ t : A[0/i] Γ ⊢ u : A[1/i]
─────────────────────────────────
Γ ⊢ (t &_i u) : Π&(i:𝔹).A
```

**Более общо**:
```
∀(x:I). Γ ⊢ tₓ : A[x/i]
────────────────────────
Γ ⊢ λ&(i:I).t_i : Π&(i:I).A
```

**Свойства**:
- Индексированное семейство типов
- Недетерминированный выбор индекса
- Контекст переиспользуется для всех вариантов

### 3.4 Зависимая аддитивная дизъюнкция (Σ⊕)

**Введение**:
```
Γ ⊢ a : I Γ ⊢ t : A[a/i]
────────────────────────────
Γ ⊢ (a, t)⊕ : Σ⊕(i:I).A
```

**Устранение**:
```
Γ ⊢ p : Σ⊕(i:I).A ∀x:I. Δ, y:A[x/i] ⊢ tₓ : C
─────────────────────────────────────────────────
Γ, Δ ⊢ case_Σ⊕ p of (x, y) ⇒ tₓ : C
```

**Свойства**:
- Индексированная сумма
- Детерминированный выбор варианта
- Тип зависит от индекса

## 4. (Ко)индуктивные типы

### 4.1 Индуктивные типы (μ)

Индуктивный тип определяется как наименьшая неподвижная точка функтора, построенного из MALL-связок с зависимыми типами:

```
μX. F(X)
```

где F : Type → Type — функтор, составленный из:
- Зависимых связок: Σ⊗, Π⅋, Π&, Σ⊕
- Базовых MALL-связок: ⊗, ⅋, &, ⊕
- Рекурсивной переменной X

#### Правила типизации

**Введение** (конструктор):
```
Γ ⊢ t : F(μX.F(X))
──────────────────────
Γ ⊢ fold(t) : μX.F(X)
```

**Устранение** (примитивная рекурсия):
```
Γ ⊢ t : μX.F(X) Δ, x:F(C) ⊢ e : C
─────────────────────────────────────
Γ, Δ ⊢ rec_μ(t, x.e) : C
```

где рекурсивные вхождения в x заменяются на рекурсивные вызовы.

#### Примеры

**Натуральные числа** (линейные):
```
Nat := μN. 1 ⊕ N
```

**Списки**:
```
List(A) := μL. 1 ⊕ (A ⊗ L)
```

**Зависимые векторы**:
```
Vec(A, n) := μV. (n =_Nat 0) & 1 ⊕ Σ⊕(m:Nat). (n =_Nat s(m)) & (A ⊗ V(m))
```

### 4.2 Коиндуктивные типы (ν)

Коиндуктивный тип определяется как наибольшая неподвижная точка:

```
νX. F(X)
```

#### Правила типизации

**Введение** (копримитивная корекурсия):
```
Γ ⊢ s : S Δ, x:S ⊢ t : F(S)
────────────────────────────────
Γ, Δ ⊢ corec_ν(s, x.t) : νX.F(X)
```

**Устранение** (деструктор):
```
Γ ⊢ t : νX.F(X)
───────────────────────
Γ ⊢ unfold(t) : F(νX.F(X))
```

#### Примеры

**Потоки**:
```
Stream(A) := νS. A & S
```

**Ленивые списки**:
```
CoList(A) := νL. 1 ⊕ (A & L)
```

### 4.3 Свойства (ко)индуктивных типов

1. **Линейность**: Все (ко)индуктивные типы соблюдают линейное использование ресурсов
2. **Позитивность**: Рекурсивная переменная встречается только в позитивных позициях
3. **Предикативность**: Индексы зависимых типов ограничены заданной вселенной
4. **Продуктивность/терминация**: Корекурсия продуктивна, рекурсия терминирует

#### Замечание о линейном отрицании и позитивности

В системе с линейным отрицанием (−)⊥ возникает вопрос: можно ли использовать X⊥ в определении (ко)индуктивных типов?

**Текущий подход** (строгая позитивность):
```
μX. F(X) где X встречается только ковариантно (позитивно)
νX. F(X) где X встречается только ковариантно (позитивно)
```

**Альтернатива с отрицанием** (требует условия вариантности):
```
μX. F(X, X⊥) где X ковариантно, X⊥ контравариантно
νX. F(X, X⊥) аналогично
```

**Проблема**: В MALL позитивность зависит от связки:
- X в A ⊗ X — позитивно (ковариантно)
- X в A ⅋ X — позитивно
- X в A & X — позитивно 
- X в A ⊕ X — позитивно
- X в X ⊗ A — позитивно
- НО: X в A ⊸ X — позитивно (X в кодомене)
- НО: X в X ⊸ A — **негативно** (X в домене, контравариантно!)
- И: X⊥ в любой позиции меняет вариантность на противоположную

**Условие корректности**: Для существования (начальной алгебры μ / терминальной коалгебры ν) функтор F должен быть **строго ковариантным** по рекурсивной переменной.

**Решение в нашей системе**:

1. **Без явного отрицания в определении**: 
 ```
 μX. F(X) и νX. F(X)
 ```
 где F использует только MALL-связки в позитивных позициях. X⊥ в определении F **не используется**.

2. **Отрицание применяется к готовому типу**:
 ```
 (μX. F(X))⊥ = νX. F(X⊥)⊥ (теоретически, при дуальности функтора)
 ```
 Но это внешнее отрицание, не внутри определения.

3. **Преимущество**: Не нужно проверять условие ковариантности — оно выполняется автоматически, так как MALL-связки (⊗, ⅋, &, ⊕) все ковариантны по обоим аргументам.

4. **Что разрешено**:
 ```
 μX. 1 ⊕ (A ⊗ X) (список)
 νX. A & X (поток)
 μX. (A ⊸ B) ⊗ X (X позитивно в A ⊸ B не участвует)
 ```

5. **Что запрещено**:
 ```
 μX. (X ⊸ A) ⊗ B (X в контравариантной позиции)
 μX. A ⊗ X⊥ (явное отрицание X)
 νX. F(X, X⊥) (смешанная вариантность)
 ```

**Вывод**: Добавление отрицания в определение (ко)индуктивных типов **не включено** в базовую систему, так как:
- Требует сложной проверки условия ковариантности
- MALL-связки естественно ковариантны, что достаточно
- Отрицание доступно как внешняя операция на готовых типах
- Сохраняет простоту и предсказуемость системы

Если требуется тип с X⊥, его можно выразить через дуальность готовых типов или через явное кодирование без встроенного отрицания.

### 4.4 Размерные типы (Sized Types)

Система включает **размерные типы** (sized types), подобные таковым в Agda, что обеспечивает более точный контроль над (ко)индуктивными типами и гарантии терминации.

#### 4.4.1 Основные понятия

**Размеры**:
```
Size := 0 | s(Size) | ∞
```

где:
- `0` — минимальный размер (пустой тип)
- `s(α)` — successor размера α
- `∞` — актуальная бесконечность (по умолчанию `∞ := 100500^100500`)

**Индуктивные типы с размером**:
```
μ^α X. F(X) : Type
```
где α — размер, ограничивающий глубину рекурсии.

**Коиндуктивные типы с размером**:
```
ν^α X. F(X) : Type
```
где α — размер, ограничивающий глубину коиндукции (глубину разворачивания).

#### 4.4.2 Правила для размерных типов

**Введение с явным размером**:
```
Γ ⊢ t : F(μ^α X.F(X))
───────────────────────────
Γ ⊢ fold^{s(α)}(t) : μ^{s(α)} X.F(X)
```

**Устранение с уменьшением размера**:
```
Γ ⊢ t : μ^{s(α)} X.F(X) Δ, x:F(μ^α X.F(X)) ⊢ e : C
──────────────────────────────────────────────────────
Γ, Δ ⊢ rec_μ(t, x.e) : C
```

Рекурсивные вызовы работают с типом меньшего размера (α вместо s(α)), что гарантирует терминацию.

**Коиндуктивное введение**:
```
Γ ⊢ s : S Δ, x:S ⊢ t : F(S)
────────────────────────────────────────
Γ, Δ ⊢ corec_ν^α(s, x.t) : ν^α X.F(X)
```

**Коиндуктивное устранение с ограничением**:
```
Γ ⊢ t : ν^{s(α)} X.F(X)
────────────────────────────────
Γ ⊢ unfold(t) : F(ν^α X.F(X))
```

#### 4.4.3 Размер по умолчанию: ультрафинитизм

**Соглашение**: Если размер не указан явно, он автоматически устанавливается в **актуальную бесконечность**:
```
∞ := 100500^100500
```

Это реализует форму **ультрафинитизма**: все типы в системе имеют конечный (хотя и огромный) размер, но для практических целей это неотличимо от бесконечности.

**Примеры**:
```
Nat := μ Nat. 1 ⊕ Nat эквивалентно Nat := μ^∞ Nat. 1 ⊕ Nat
Stream(A) := νS. A & S эквивалентно Stream(A) := ν^∞ S. A & S
```

**Философия**: 
- Все реальные вычисления конечны
- ∞ = 100500^100500 достаточно велико для любых практических целей
- Но теоретически это конечное число, что избегает парадоксов актуальной бесконечности
- Позволяет рассуждать о "почти бесконечных" структурах финитными методами

#### 4.4.4 Субтипирование по размеру

Существует отношение субтипирования:
```
α ≤ β ⟹ μ^α X.F(X) <: μ^β X.F(X)
α ≤ β ⟹ ν^β X.F(X) <: ν^α X.F(X) (контравариантно!)
```

**Правило subsumption**:
```
Γ ⊢ t : A A <: B
────────────────────
Γ ⊢ t : B
```

#### 4.4.5 Практическое использование

**Гарантия терминации**:
```
countdown : μ^n Nat → 1
countdown = rec_μ(λx. case x of
 | inl(*) → *
 | inr(n') → countdown(n')) -- n' : μ^{n-1} Nat, терминирует при n=0
```

**Ограниченная продуктивность**:
```
take : ℕ → ν^∞ S. A & S → μ^n List. 1 ⊕ (A ⊗ List)
```
Берёт конечный префикс коиндуктивного потока.

**Размерные полиморфизмы**:
```
map : ∀α. (A → B) → μ^α List(A) → μ^α List(B)
```
Функция сохраняет размер структуры данных.

#### 4.4.6 Связь с зависимыми типами

Размеры могут быть зависимыми:
```
Vec : Type → Size → Type
Vec(A, 0) := 1
Vec(A, s(n)) := A ⊗ Vec(A, n)
```

Или с Σ и Π:
```
SizedList(A) := Σ⊗(n:Size). μ^n L. 1 ⊕ (A ⊗ L)
```
Список с явным размером.

## 5. Дифференцирование (DILL)

Дифференцирование в DILL основано на комонаде ! из раздела 5, добавляя оператор производной.

### 6.2 Дифференциальный оператор (∂)

Вводим оператор дифференцирования ∂, который вычисляет "производную" типа:

```
∂(A) — тип "контекстов с дыркой типа A"
```

**Связь с размерными типами**: Наличие размерных типов (раздел 4.4) и ультрафинитизм (∞ = 100500^100500) позволяют корректно определить дифференцирование для **любых** (ко)индуктивных типов. Финитность гарантирует, что производная всегда определена и вычислима:
- Для индуктивных типов μ^α X.F(X), производная ∂(μ^α X.F(X)) существует и конечна при α < ∞
- Для коиндуктивных типов ν^α X.F(X), производная также определена благодаря ограничению размера α
- Размер по умолчанию ∞ = 100500^100500 достаточно велик для практических вычислений, но конечен для теоретических гарантий

Это ключевое преимущество ультрафинитного подхода: дифференцирование работает универсально для всех типов системы без специальных ограничений.

#### Правила дифференцирования типов

1. **Базовые случаи**:
 - ∂(B) = 0 для базового типа B
 - ∂(1) = 0
 - ∂(0) = 0

2. **Тензорное произведение**:
 ```
 ∂(A ⊗ B) = (∂A ⊗ B) ⊕ (A ⊗ ∂B)
 ```

3. **Линейная функция**:
 ```
 ∂(A ⊸ B) = A ⊗ ∂B
 ```

4. **Аддитивная конъюнкция**:
 ```
 ∂(A & B) = ∂A & ∂B
 ```

5. **Аддитивная дизъюнкция**:
 ```
 ∂(A ⊕ B) = ∂A ⊕ ∂B
 ```

6. **Зависимые типы**:
 - ∂(Σ⊗(x:A).B) = (∂A ⊗_Σ B) ⊕ Σ⊗(x:A).∂B
 - ∂(Π&(i:I).A) = Π&(i:I).∂A

7. **Индуктивные типы**:
 ```
 ∂(μX. F(X)) = Σ⊗(c : F(μX.F(X))). ∂F(μX.F(X))
 ```

### 6.3 Правила типизации для дифференцирования

**Введение дифференциала**:
```
Γ ⊢ t : A Δ ⊢ u : ∂A
───────────────────────────
Γ, Δ ⊢ 𝔻(t, u) : ∂A
```

**Применение производной**:
```
Γ ⊢ f : !A ⊸ B Δ ⊢ a : A Θ ⊢ h : ∂A
───────────────────────────────────────────
Γ, Δ, Θ ⊢ ∂f(a)(h) : ∂B
```

Интерпретация: ∂f — линеаризация функции f в точке a.

### 6.4 Взаимодействие с (ко)индуктивными типами

Для индуктивного типа μX.F(X):
- **Производная конструктора**: Дифференцирование fold даёт доступ к "производной структуре данных"
- **Zipper-структуры**: ∂(μX.F(X)) представляет zipper над структурой данных

Для коиндуктивного типа νX.F(X):
- **Производная деструктора**: Дифференцирование unfold
- **Контексты наблюдения**: ∂(νX.F(X)) описывает контекст наблюдения

## 6. Семантика и вычисления

### 7.1 Редукции

Основные β-редукции:

1. ```let x ⊗ y = (t ⊗ u) in v → v[t/x, u/y]```
2. ```π_i((t, u)) → t``` (для i=1) или u (для i=2)
3. ```case(inl(t), x.u, y.v) → u[t/x]```
4. ```rec_μ(fold(t), x.e) → e[t/x]```
5. ```unfold(corec_ν(s, x.t)) → t[s/x]```

Для дифференцирования:
- ```∂(λx.t)(a)(h) → t[a/x, h/∂x]```

### 7.2 Свойства системы

1. **Subject reduction**: Если Γ ⊢ t : A и t → t', то Γ ⊢ t' : A
2. **Слабая нормализация**: Все хорошо типизированные термы сводятся к нормальной форме (для индуктивных типов)
3. **Линейность**: Каждая переменная используется ровно один раз (кроме переменных под !)
4. **Согласованность**: Не существует терма типа 0

## 7. Вселенные типов

Вводим иерархию вселенных 𝓤₀ : 𝓤₁ : 𝓤₂ : … для обеспечения консистентности при работе с зависимыми типами и (ко)индуктивными определениями.

**Правила**:

```
A : 𝓤ᵢ B : 𝓤ⱼ
──────────────────────
A ⊗ B : 𝓤_{max(i,j)}
```

```
A : 𝓤ᵢ x:A ⊢ B : 𝓤ⱼ
─────────────────────────────
Σ⊗(x:A).B : 𝓤_{max(i,j)}
```

```
F : 𝓤ᵢ → 𝓤ᵢ
──────────────
μX.F(X) : 𝓤ᵢ
```

## 8. Примеры и приложения

### 9.1 Линейные натуральные числа с дифференцированием

```
Nat := μN. 1 ⊕ N
```

```
∂(Nat) = Σ⊗(c : 1 ⊕ Nat). (0 ⊕ ∂(Nat))
```

Упрощённо: ∂(Nat) ≅ Nat (zipper — это позиция в унарном числе)

### 9.2 Зависимые векторы с дифференцированием

```
Vec(A, n) := μV. (n = 0) & 1 ⊕ (n > 0) & (A ⊗ V(n-1))
```

Производная даёт структуру "вектор с дыркой" — zipper над вектором.

### 9.3 Корекурсивные потоки с дифференцированием

```
Stream(A) := νS. A & S
```

```
∂(Stream(A)) = ∂A & Stream(A)
```

Описывает "контекст наблюдения" за потоком с выделенным элементом.

## 9. Модальности

**Важное замечание**: Базовая система типов (разделы 1-8) определяется через MALL-связки, зависимые типы, (ко)индуктивные типы и дифференцирование. Модальности **не являются частью базовой системы**, но структура позволяет их естественным образом определить через (ко)индуктивные типы.

### 9.1 Экспоненциальные модальности ! и ?

Для интеграции нелинейности в линейную систему типов вводятся экспоненциальные модальности:
- **!** (of course) — комонада, позволяющая дублирование и удаление
- **?** (why not) — монада, структурно симметричная к !


### 9.1.1 Комонада ! (of course)

#### 5.1.1 Основное определение: коиндуктивный тип с аддитивным выбором

```
!A := νX. 1 ⊕ (A & X)
```

**Интуиция**: Элемент типа !A — это коиндуктивный поток с выбором на каждом шаге:
- `unfold(x) = inl(*)` : завершение потока (предоставлено 0 копий A)
- `unfold(x) = inr(a, x')` где a:A и x':!A : предоставление элемента a и продолжения x'
 - Но здесь `(a, x') : A & !A` — это аддитивная конъюнкция!
 - Потребитель может недетерминированно выбрать либо π₁ (взять a), либо π₂ (продолжить разворачивать)

**Ключевое отличие от списков**:
```
Stream(A) = νX. A & X -- детерминированный поток (всегда есть элемент)
!A = νX. 1 ⊕ (A & X) -- может закончиться (через inl) или продолжиться (через inr)
```

**Семантика**: Коиндуктивный тип описывает потенциально бесконечный "поток возможностей" предоставить A. & означает, что на каждом шаге потребитель выбирает: взять элемент или продолжить.

#### 5.1.2 Альтернативные построения

**Индексированное произведение**:
```
!A := Π&(n:ℕ). (A^⊗n)
```
Семейство всех арностей одновременно. Более абстрактное, но эквивалентное определение.

**Через коммутативную моноиду**:
```
!A := νX. ⊤ & (A ⊗ X)
```
Альтернативная коиндуктивная формулировка.

**Связь между определениями**: В присутствии правильных изоморфизмов, эти определения эквивалентны. Основное определение через νX. 1 ⊕ (A & X) выбрано для структурной дуальности с ?.

#### 5.1.3 Правила типизации для !

**Введение** (построение через корекурсию):
```
───────────────────
Γ ⊢ empty! : !A
```
где `empty! = fold(inl(*))` — пустой поток.

```
Γ ⊢ a : A Δ ⊢ xs : !A
─────────────────────────
Γ, Δ ⊢ offer(a, xs) : !A
```
где `offer(a, xs) = fold(inr((a, xs)))` — предложение элемента a с продолжением xs.

**Устранение** (обработка через анаморфизм):
```
Γ ⊢ t : !A Δ ⊢ e₀ : B Θ ⊢ e₁ : A & !A → B
────────────────────────────────────────────────
Γ, Δ, Θ ⊢ match t with
 | empty! → e₀
 | offer(choice) → e₁(choice) : B
```

где `choice : A & !A` и потребитель выбирает π₁ (взять A) или π₂ (продолжить с !A).

**Комонадные операции**:

**Dereliction** ε : !A → A (коединица):
```
derel : !A → A
derel(x) = match unfold(x) with
 | inl(*) → ошибка (пустой поток)
 | inr(choice) → π₁(choice) -- выбираем элемент
```

**Contraction** δ : !A → !!A (коумножение):
```
copy : !A → !!A
copy(x) = fold(inr((x, copy(x)))) -- поток потоков
```

**Weakening** w : !A → 1:
```
discard : !A → 1
discard(x) = *
```

**Promotion**:
```
x₁:!A₁, ..., xₙ:!Aₙ ⊢ t : B
──────────────────────────────────────────
x₁:!A₁, ..., xₙ:!Aₙ ⊢ promote(t) : !B
```

#### 5.1.4 Примеры использования !

**Функция с повторным использованием аргумента**:
```
dup : !(A ⊗ A) ⊸ !A
dup(x) = let z = derel(x) in
 let (a₁, a₂) = z in
 promote(a₁) -- используем только первый элемент
```

**Итерация**:
```
iterate : ℕ → !(A ⊸ A) → A → A
iterate(0, f, x) = x
iterate(n+1, f, x) = derel(f)(iterate(n, copy₁(f), x))
```

где copy₁ копирует первую компоненту при contraction.

### 9.1.2 Монада ? (why not)

#### 5.2.1 Основное определение: индуктивный тип мультимножеств

```
?A := μX. ⊤ ⊕ (A ⊗ X)
```

**Интуиция**: Элемент типа ?A — это конечное мультимножество элементов типа A. Это индуктивный тип с двумя конструкторами:
- `fold(inl(*))` : ?A — пустое мультимножество
- `fold(inr(a, xs))` где a:A и xs:?A — добавление элемента a к мультимножеству xs

**Структура**:
```
?A = { ∅, {a₁}, {a₁,a₂}, {a₁,a₂,a₃}, ... }
```

Каждое мультимножество конечно (индуктивность), но может содержать произвольное количество элементов. Это дуально к ! в следующем смысле:
- **!A**: можем _запросить_ произвольное количество копий (коиндуктивно)
- **?A**: можем _предоставить_ произвольное количество копий (индуктивно)

**Сравнение с списками**:
```
List(A) = μX. 1 ⊕ (A ⊗ X) -- упорядоченный список
?A = μX. ⊤ ⊕ (A ⊗ X) -- неупорядоченное мультимножество (⊤ вместо 1)
```

Разница: для ?A порядок не важен (коммутативность), что соответствует коммутативной структуре моноиды для !.

#### 5.2.2 Альтернативные построения (кратко)

**Через индексированную сумму**:
```
?A := Σ⊕(n:ℕ). (A^⊗n / Sₙ)
```
где Sₙ — симметрическая группа (факторизация по перестановкам). Явно выражает конечность и неупорядоченность.

**Через дуальность**:
```
?A := (!(A⊥))⊥
```
Определение через линейное отрицание комонады !, обеспечивающее корректную дуальность.

#### 5.2.3 Правила типизации для ?

**Введение** (построение мультимножества):
```
──────────────────
Γ ⊢ empty : ?A
```
где `empty = fold(inl(*))`.

```
Γ ⊢ a : A Δ ⊢ xs : ?A
─────────────────────────
Γ, Δ ⊢ cons(a, xs) : ?A
```
где `cons(a, xs) = fold(inr(a, xs))`.

**Устранение** (обработка мультимножества):
```
Γ ⊢ t : ?A Δ ⊢ e₀ : B Θ, x:A, xs:?A ⊢ e₁ : B
──────────────────────────────────────────────────
Γ, Δ, Θ ⊢ match t with
 | empty → e₀
 | cons(x, xs) → e₁ : B
```

**Монадные операции**:

**Единица** (одноэлементное мультимножество):
```
Γ ⊢ a : A
─────────────────────────
Γ ⊢ unit(a) : ?A
```
где `unit(a) = cons(a, empty)`.

**Умножение** (flatten — схлопывание мультимножества мультимножеств):
```
Γ ⊢ t : ??A
─────────────────
Γ ⊢ flatten(t) : ?A
```
где flatten рекурсивно склеивает все внутренние мультимножества в одно:
```
flatten(empty) = empty
flatten(cons(xs, xss)) = append(xs, flatten(xss))
```

#### 5.2.4 Операции через основное определение

**Unit** (η монады):
```
unit : A → ?A
unit(a) = fold(inr(a, fold(inl(*))))
```

**Flatten** (μ монады):
```
flatten : ??A → ?A
flatten = rec_μ(λt. match t with
 | inl(*) → fold(inl(*)) -- пустое мультимножество мультимножеств
 | inr(xs, xss) → append(xs, flatten(xss)))
```

где `append : ?A → ?A → ?A` конкатенирует два мультимножества:
```
append(xs, ys) = rec_μ(xs, λt. match t with
 | inl(*) → ys
 | inr(a, xs') → fold(inr(a, append(xs', ys))))
```

### 9.1.3 Взаимосвязь ! и ? — сопряжение vs дуальность

#### 5.3.1 Сопряжение ! ⊣ ? (основное свойство)

**Изоморфизм hom-множеств**:
```
!A ⊸ B ≅ A → ?B
```
где A → B — сокращение для !A ⊸ B (интуиционистская импликация).

**Явная конструкция сопряжения**:

Для f : !A → B строим curry(f) : A → ?B:
```
curry(f)(a) = unit(f(promote(a)))
```
где promote(a) создаёт минимальный элемент !A.

Для g : A → ?B строим uncurry(g) : !A → B:
```
uncurry(g)(x) = match g(derel(x)) of
 | empty → ошибка_или_дефолт
 | cons(b, _) → b
```

**Интуиция сопряжения**:
- f : !A → B может запросить любое количество копий A для производства одного B
- g : A → ?B из одного A производит мультимножество результатов B
- Эти возможности **функционально дуальны**: "потреблять много" ↔ "производить много"

#### 5.3.2 Проверка точной категорной дуальности

Теперь проверим дуальность для основных определений:

**Для !A = νX. 1 ⊕ (A & X)**:
```
(!A)⊥ = (νX. 1 ⊕ (A & X))⊥
 ≅ μX. (1 ⊕ (A & X))⊥ [дуальность ν/μ]
 ≅ μX. (1⊥ & (A & X)⊥) [де Морган: (A⊕B)⊥ = A⊥ & B⊥]
 ≅ μX. (⊥ & (A⊥ ⊕ X)) [де Морган: (A&B)⊥ = A⊥ ⊕ B⊥, 1⊥ = ⊥]
```

Теперь используем свойство ⊥ & B. В линейной логике:
- ⊥ & B ≅ 0 (нет конструктора, так как первая альтернатива невозможна)

Хм, это даёт μX. 0, что неправильно.

**Альтернативный анализ**: Проблема в том, что ⊥ & B не упрощается просто. Давайте используем другое свойство.

На самом деле, для корректной дуальности нужно использовать:
```
⊥ & B ≅ B (если рассматривать ⊥ как "всегда истинное" в контексте &)
```

Тогда:
```
(!A)⊥ ≅ μX. (⊥ & (A⊥ ⊕ X))
 ≅ μX. (A⊥ ⊕ X)
```

Но это не равно μX. ⊤ ⊕ (A⊥ ⊗ X) = ?(A⊥)!

**Корректировка**: Нужно использовать другое тождество. Если 1⊥ ≅ ⊥ и ⊥⊥ ≅ ⊤, тогда для правильной дуальности нужно:

```
(!A)⊥ ≅ μX. (⊥ & (A⊥ ⊕ X))
```

А для ?A:
```
(?A)⊥ = (μX. ⊤ ⊕ (A ⊗ X))⊥
 ≅ νX. (⊤ ⊕ (A ⊗ X))⊥
 ≅ νX. (0 & (A⊥ ⅋ X)) [⊤⊥ = 0, (A⊕B)⊥ = A⊥&B⊥, (A⊗B)⊥ = A⊥⅋B⊥]
```

И 0 & B... это проблема.

**Вывод**: Даже с коиндуктивным определением ! = νX. 1 ⊕ (A & X), точная категорная дуальность с ? = μX. ⊤ ⊕ (A ⊗ X) **все еще не работает** из-за несоответствия единиц (1/⊤ и ⊥/0).

#### 5.3.3 Структурная симметрия vs точная дуальность

С основными определениями:
```
!A := νX. 1 ⊕ (A & X) [коиндуктивное]
?A := μX. ⊤ ⊕ (A ⊗ X) [индуктивное]
```

**Структурная симметрия**:
- ν ↔ μ (коиндукция ↔ индукция)
- & ↔ ⊗ (аддитивная конъюнкция ↔ мультипликативная конъюнкция)
- ⊕ остаётся ⊕ (детерминированный выбор)
- Но: 1 ↔ ⊤ (разные единицы!)

Эта структурная симметрия обеспечивает:

 **Функциональную дуальность** через сопряжение ! ⊣ ?:
```
!A ⊸ B ≅ A → ?B
```

 **Вычислительную согласованность**: операции ! и ? зеркально симметричны

 **Точную категорную дуальность** через (−)⊥:
```
(?A)⊥ = (μX. ⊤ ⊕ (A ⊗ X))⊥
 ≅ νX. (0 & (A⊥ ⅋ X))
 ≠ νX. 1 ⊕ (A⊥ & X) = !(A⊥)
```

Несоответствие из-за: ⊤⊥ = 0 ≠ 1 и (A⊗B)⊥ = A⊥ ⅋ B⊥ ≠ A⊥ & B⊥.

**Возможность точной дуальности**: Для точной категорной дуальности можно использовать:
```
!A := νX. 1 ⊕ (A & X)
?A := μX. ⊥ & (A ⊕ X)
```

Тогда (!A)⊥ = ?(A⊥) выполняется точно. Но μX. ⊥ & (A ⊕ X) имеет странную семантику: ⊥ в & означает "невозможная альтернатива", так что эффективно это ≅ μX. (A ⊕ X), что теряет структуру мультимножества.

**Практический вывод**:
1. Основные определения с 1 и ⊤ обеспечивают **структурную симметрию** и **функциональную дуальность** (! ⊣ ?)
2. Точная категорная дуальность через (−)⊥ требует жертв в вычислительной семантике
3. Для всех практических применений достаточно сопряжения ! ⊣ ?
4. Структурная симметрия (ν↔μ, &↔⊗) более важна для понимания системы, чем точная категорная дуальность

### 9.1.4 Взаимодействие с зависимыми типами

#### 5.4.1 ! и зависимые типы

**Для Π&**:
```
!(Π&(i:I).A) ≅ Π&(i:I).!A
```
Комонада ! коммутирует с индексированными произведениями, так как оба являются пределами.

**Для Σ⊗** (с осторожностью):
```
!(Σ⊗(x:A).B) → Σ⊗(x:!A).!B
```
Морфизм существует, но обратный не всегда корректен из-за зависимости.

**Для ⊗**:
```
!(A ⊗ B) ≅ !A ⊗ !B
```
Комонада ! — моноидальный функтор.

#### 5.4.2 ? и зависимые типы

**Для ⊕**:
```
?(A ⊕ B) → ?A ⊕ ?B
```
Морфизм отображает мультимножество со смешанными элементами в выбор между мультимножествами. Но обратное не всегда верно — мультимножество может содержать элементы обоих типов.

**Для Σ⊕**:
```
?(Σ⊕(i:I).A) → Σ⊕(i:I).?A
```
Аналогично — можем разделить мультимножество по индексам, но не всегда можем объединить обратно.

**Важно**: Монада ? (мультимножества) **не** коммутирует с большинством структур, так как мультимножество может содержать гетерогенную смесь элементов.

### 9.1.5 Моноидальные функторы

#### 5.5.1 ! как моноидальный функтор

```
m : !A ⊗ !B → !(A ⊗ B)
m(x, y) = promote(derel(x) ⊗ derel(y))
```

```
e : 1 → !1
e(*) = promote(*)
```

Делает ! слабым моноидальным функтором.

#### 5.5.2 ? как комоноидальный функтор

```
Δ : ?(A & B) → ?A & ?B
Δ(x) = (corel(π₁(flatten(x))), corel(π₂(flatten(x))))
```

Делает ? слабым комоноидальным функтором относительно &.

### 9.1.6 Вложенная линейность

Система с ! и ? позволяет выразить вложенные уровни линейности:

**Уровень 0** (полностью линейные типы): A, B, A ⊗ B, ...
**Уровень 1** (один уровень нелинейности): !A, ?(A ⊗ B), ...
**Уровень 2** (вложенная нелинейность): !!A, !(?A), ...

**Пример**: Матрица с линейными коэффициентами, но нелинейной структурой:
```
Matrix(A) := !(Vec(A) ⊗ Vec(A))
```

### 9.1.7 Вычислительная семантика

#### 5.7.1 ! и sharing/memoization

Значение типа !A может храниться в памяти и переиспользоваться:
- **Copy-on-write**: физическая копия создаётся только при модификации
- **Reference counting**: счётчик ссылок управляет временем жизни
- **Garbage collection**: автоматическое освобождение неиспользуемых значений

#### 5.7.2 ? и недетерминизм

Значение типа ?A представляет недетерминистический выбор:
- **Backtracking**: возврат к предыдущим выборам при неудаче
- **Параллельные вычисления**: исследование всех вариантов параллельно
- **Вероятностные вычисления**: выбор с вероятностным распределением

### 9.1.8 Примеры полных определений

#### 5.8.1 Натуральные числа с обычной семантикой

```
Nat := !Nat_lin
Nat_lin := μN. 1 ⊕ N

zero : Nat
zero = promote(fold(inl(*)))

succ : Nat → Nat
succ(n) = promote(fold(inr(derel(n))))

plus : Nat → Nat → Nat
plus(m, n) = rec_μ(derel(m),
 λx. case x of
 | inl(*) → n
 | inr(m') → succ(promote(plus(derel(promote(m')), copy(n)))))
```

#### 5.8.2 Списки с произвольным доступом

```
List(A) := !(List_lin(A))
List_lin(A) := μL. 1 ⊕ (A ⊗ L)

map : (A → B) → List(A) → List(B)
map(f, xs) = promote(rec_μ(derel(xs),
 λx. case x of
 | inl(*) → fold(inl(*))
 | inr(a, xs') → fold(inr(derel(f)(a), map(copy(f), promote(xs'))))))
```


### 9.2 Другие модальности: расширения системы типов

Кроме ! и ?, структура системы типов позволяет определить множество других модальностей. **Все они не входят в базовую систему**, но могут быть добавлены через (ко)индуктивные типы.

#### 9.2.1 Временные модальности

**Всегда в будущем (□)**: 
```
□A := νX. A & X
```
Правила: проекция π₂ даёт переход к следующему моменту времени.

**Когда-нибудь в будущем (◇)**:
```
◇A := μX. A ⊕ X
```
Правила: либо A сейчас (inl), либо A позже (inr).

**С зависимыми типами богаче**: □_t A := Π&(t':Time, t'≥t).A(t') — индексирование временем; □^d A — с длительностью интервала; Σ⊗(t:Time).A(t) U_t B(t) — until с зависимостью от момента времени.

#### 9.2.2 Пространственные модальности

**Везде (□_S)**:
```
□_S A := Π&(l:Location).A(l)
```
Используя Π& из раздела 3.3.

**Где-то (◇_S)**:
```
◇_S A := Σ⊕(l:Location).A(l)
```
Используя Σ⊕ из раздела 3.4.

**С зависимыми типами богаче**: □_r A := Π&(l:Location, d(l,here)≤r).A(l) — в радиусе r с метрикой расстояния; Σ⊗(r:Region).Π&(l:r).A(l) — по регионам; Σ⊗(l:Location).(A(l) ⊗ Resources(l)) — с локальными ресурсами.

#### 9.2.3 Деонтические модальности

**Обязательно (O)**:
```
O A := νX. A & (X ⊕ Violation)
```
Коиндуктивная последовательность проверок обязательства.

**Разрешено (P)**:
```
P A := μX. A ⊕ (X & Grant)
```
Индуктивная последовательность разрешений.

**С зависимыми типами богаче**: O_c A := Σ⊗(c:Context).(A(c) & Obligation(c)) — обязательство зависит от контекста; Π&(p:Priority).O_p A — с приоритетами; Σ⊕(a:Agent).O_a A — кто обязан; Π⅋(cond:Condition).O A — условное обязательство.

#### 9.2.4 Алетические модальности

**Необходимо (□_N)**:
```
□_N A := Π&(w:World).A(w)
```
A истинно во всех возможных мирах.

**Возможно (◇_N)**:
```
◇_N A := Σ⊕(w:World).A(w)
```
A истинно в некотором мире.

**С зависимыми типами богаче**: Π&(w:World, R(w₀,w)).A(w) — через отношение доступности R; Σ⊗(n:ℕ).□ⁿ A — с указанием модальной глубины; Π&(w:World).A(w) ⊗ Prob(w) — взвешенная необходимость с вероятностями миров.

#### 9.2.5 Эпистемические модальности

**Знание агента (K_a)**:
```
K_a A := Π&(s:State_a).A(s)
```

**Общее знание (C)**:
```
C A := νX. A & (Π&(a:Agent).K_a X)
```
Коиндуктивное: все знают, что все знают, что...

**С зависимыми типами богаче**: K_a(Σ⊗(b:Agent).K_b A) — знание о знании других; Σ⊗(G:Group).D_G A — распределённое знание группы; K_a^t := Π&(s:State_a(t)).A(s) — временное знание; Σ⊗(c:Confidence).K_a A ⊗ c — с уровнем уверенности; Π⅋(e:Evidence).K_a A — условное знание.

#### 9.2.6 Другие модальности

**Вероятностные**: 
```
□_p A := A ⊗ Prob(p)
D A := Σ⊕(n:ℕ).(A^⊗n ⊗ Distribution(n))
```
**С зависимыми типами богаче**: Σ⊗(e:Evidence).(A ⊗ Prob(A|e)) — условная вероятность; Π⅋(obs:Observation).□_p A → □_{p'} A — байесовское обновление; Σ⊗(θ:Parameters).D_θ A — параметризованные распределения.

**LTL (Until, Release)**:
```
A U B := μX. B ⊕ (A ⊗ X)
A R B := νX. B & (A ⅋ X)
```
**С зависимыми типами богаче**: Σ⊗(n:ℕ).(A U_≤n B) — bounded until; Π⅋(P:Predicate).A U_P B — с предикатом на каждом шаге; Σ⊗(t:Time).A U_{[0,t]} B — метрическое until.

**Афfinные/Релевантные**:
```
◯A := A ⊕ 1 -- можно не использовать
◉A := μX. A ⊕ (A ⊗ X) -- минимум одно использование
```
**С зависимыми типами богаче**: Π⅋(cond:Condition).(A ⊕ 1(cond)) — условная афfinность; Σ⊗(n:ℕ_{>0}).A^⊗n — точная релевантность; Σ⊗(c:Context).(A(c) ⊕ 1) — контекстная афfinность.

**Градуированные**:
```
□_n A := A^⊗n -- ровно n использований
□_≤n A := Σ⊕(k:ℕ_{≤n}).A^⊗k -- до n использований
□_≥n A := A^⊗n ⊗ !A -- минимум n использований
```
**С зависимыми типами богаче**: Σ⊗(n:ℕ).□_n(A(n)) — градация зависит от типа; Σ⊗(v:Vector(Resource)).A^⊗v — многомерные ресурсы; Σ⊗(n,m:ℕ, n≤m).□_{[n,m]} A — интервальная градация; Π⅋(params:Params).□_{f(params)} A — параметризованная градация.

### 9.3 Общий принцип определения модальностей

**Правило**: Любая модальность, выразимая через:
- (Ко)индуктивные типы (μ, ν)
- MALL-связки (⊗, ⅋, &, ⊕)
- Зависимые типы (Σ⊗, Π⅋, Π&, Σ⊕)

может быть определена в системе.

**Типизация**: Для модальности M добавляются:
1. Правила введения (как построить MA)
2. Правила устранения (как использовать MA)
3. Монадная/комонадная структура (если применимо)

**Композиция**: Модальности могут композироваться через функториальную структуру:
```
(!□)A = !(□A) -- бесконечное использование временного потока
(□!)A = □(!A) -- поток бесконечно используемых значений
```

**Роль зависимых типов**: Четыре зависимых связки значительно обогащают модальности:
- **Σ⊗** — параметризация модальностей (зависимость от параметров, состояний)
- **Π⅋** — условные модальности (модальность при условии)
- **Π&** — индексированные модальности (по времени, пространству, мирам, агентам)
- **Σ⊕** — выбор варианта с зависимостью (индексированные дизъюнкции)

Без зависимых типов модальности были бы значительно менее выразительны. Зависимые типы позволяют создавать богатые модальные системы, адаптированные к конкретным доменам.

### 9.4 Резюме по модальностям

**Архитектура системы**:
- **Разделы 1-8**: Базовая система (MALL, зависимые типы, (ко)индукция, дифференцирование)
- **Раздел 9**: Модальности (расширения на основе базовой структуры)

**Преимущества**:
- Единообразное определение различных модальностей
- Переиспользование базовых конструкций
- Расширяемость без изменения ядра системы

**Применения**:
- Темпоральная логика программ
- Распределённые системы (пространственные модальности)
- Мультиагентные системы (эпистемические модальности)
- Вероятностное программирование
- Системы с эффектами

## 10. Заключение

### Архитектура системы типов

Система организована в два уровня:

**Базовая система (разделы 1-8)**:
- **Линейная логика MALL** для управления ресурсами (⊗, ⅋, &, ⊕)
- **Зависимые типы** из четырёх связок (Σ⊗, Π⅋, Π&, Σ⊕) для точной спецификации
- **(Ко)индуктивные типы** (μ, ν) для рекурсивных структур данных
- **Размерные типы** (μ^α, ν^α) с ультрафинитизмом (∞ = 100500^100500)
- **Дифференцирование** (∂) для работы с контекстами и производными структурами

**Расширения через модальности (раздел 9)**:
- **Экспоненциальные**: ! (of course), ? (why not)
- **Временные**: □ (всегда), ◇ (когда-нибудь), U (until), R (release)
- **Пространственные**: □_S (везде), ◇_S (где-то)
- **Деонтические**: O (обязательно), P (разрешено)
- **Алетические**: □_N (необходимо), ◇_N (возможно)
- **Эпистемические**: K_a (знание), C (общее знание)
- **И другие**: вероятностные, градуированные, афfinные

### Ключевые свойства

1. **Четыре зависимых связки** дают богатую систему для спецификации различных видов зависимостей:
 - Σ⊗ — мультипликативная зависимая сумма
 - Π⅋ — мультипликативное зависимое произведение
 - Π& — индексированное произведение (предел)
 - Σ⊕ — индексированная копроизведение (копредел)

2. **Модальности не встроены**, а **определяются через базовую структуру**:
 - Используют (ко)индуктивные типы
 - Используют MALL-связки и зависимые типы
 - Могут добавляться по необходимости

3. **Размерные типы и ультрафинитизм**:
 - Каждый тип имеет размер (по умолчанию ∞ = 100500^100500)
 - Гарантирует терминацию индуктивных типов и продуктивность коиндуктивных
 - Обеспечивает корректность дифференцирования для всех типов

4. **Дифференцирование** взаимодействует со всеми уровнями:
 - Даёт zipper-структуры для (ко)индуктивных типов
 - Позволяет инкрементальные вычисления
 - Универсально применимо благодаря финитности размерных типов
 - Взаимодействует с модальностями (∂(!A) ≅ !A ⊗ A)

### Теоретические свойства

Система обладает:
- **Согласованностью** (не существует терма типа 0)
- **Subject reduction** (сохранение типов при редукции)
- **Слабой нормализацией** (для индуктивных типов)
- **Линейностью** (каждая переменная используется ровно раз, кроме модальностей)

### Практические применения

**Программирование**:
- Управление ресурсами (линейные типы)
- Zipper-структуры (через дифференцирование)
- Инкрементальные вычисления
- Эффекты и состояние (через модальности)

**Верификация**:
- Темпоральная логика программ
- Логика знаний для мультиагентных систем
- Пространственное рассуждение в распределённых системах

**Специализированные области**:
- Квантовые вычисления (линейность = запрет клонирования)
- Вероятностное программирование (через вероятностные модальности)
- Системы реального времени (временные модальности)
